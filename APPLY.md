# Apply this portfolio overlay

Copy these files to the root of the existing `team4958_customs` repository, replacing the files with the same names:

- `README.md`
- `pyproject.toml`
- `LICENSE`
- `.gitignore`

`PORTFOLIO_NOTES.md` is optional. Keep it if you want the cleanup rationale visible in the repository; otherwise it can remain local.

The Python source files should remain unchanged.

Suggested GitHub **About** text:

> Early independently written Python package with reusable helpers for async work, MySQL and project scaffolding.

Suggested topics:

`python`, `python-library`, `mysql`, `asyncio`, `threading`, `project-template`, `learning-project`
